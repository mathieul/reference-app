<tr>
  <td>{{contact.id}}</td>
  <td class="data">
    {{#if isEditing}}
      {{view App.EditContactView}}
    {{else}}
      {{contact.fullName}}
    {{/if}}
  </td>
  <td class="commands">
    {{#unless isEditing}}
      <a href="#" {{action "showEdit"}}>Edit</a>
      <a href="#" {{action "destroyRecord"}}>Remove</a>
    {{/unless}}
  </td>
</tr>
